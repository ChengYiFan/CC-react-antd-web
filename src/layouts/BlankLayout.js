export default (props) => <div style={{padding: 20, background:'#f0f0f0'}}>
    {props.children}
</div>

// import React from 'react';

// const Layout = ({ children }) => <div>{children}</div>;

// export default Layout;
